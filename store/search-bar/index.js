export const state = () => ({
  searchBarSelectedItem: null,
})

export const getters = {
}

export const mutations = {
  setSearchBarSelectedItem(state, searchBarSelectedItem) {
    state.searchBarSelectedItem = searchBarSelectedItem;
  },
}

export const actions = {
}
